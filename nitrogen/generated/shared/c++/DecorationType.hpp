///
/// DecorationType.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroreadium {

  /**
   * An enum which can be represented as a JavaScript union (DecorationType).
   */
  enum class DecorationType {
    HIGHLIGHT      SWIFT_NAME(highlight) = 0,
    UNDERLINE      SWIFT_NAME(underline) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroreadium

namespace margelo::nitro {

  using namespace margelo::nitro::nitroreadium;

  // C++ DecorationType <> JS DecorationType (union)
  template <>
  struct JSIConverter<DecorationType> final {
    static inline DecorationType fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("highlight"): return DecorationType::HIGHLIGHT;
        case hashString("underline"): return DecorationType::UNDERLINE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum DecorationType - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, DecorationType arg) {
      switch (arg) {
        case DecorationType::HIGHLIGHT: return JSIConverter<std::string>::toJSI(runtime, "highlight");
        case DecorationType::UNDERLINE: return JSIConverter<std::string>::toJSI(runtime, "underline");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert DecorationType to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("highlight"):
        case hashString("underline"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
