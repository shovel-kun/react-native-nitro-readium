///
/// Spread.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::nitroreadium {

  /**
   * An enum which can be represented as a JavaScript union (Spread).
   */
  enum class Spread {
    AUTO      SWIFT_NAME(auto) = 0,
    NEVER      SWIFT_NAME(never) = 1,
    ALWAYS      SWIFT_NAME(always) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::nitroreadium

namespace margelo::nitro {

  using namespace margelo::nitro::nitroreadium;

  // C++ Spread <> JS Spread (union)
  template <>
  struct JSIConverter<Spread> final {
    static inline Spread fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("auto"): return Spread::AUTO;
        case hashString("never"): return Spread::NEVER;
        case hashString("always"): return Spread::ALWAYS;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum Spread - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, Spread arg) {
      switch (arg) {
        case Spread::AUTO: return JSIConverter<std::string>::toJSI(runtime, "auto");
        case Spread::NEVER: return JSIConverter<std::string>::toJSI(runtime, "never");
        case Spread::ALWAYS: return JSIConverter<std::string>::toJSI(runtime, "always");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert Spread to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("auto"):
        case hashString("never"):
        case hashString("always"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
